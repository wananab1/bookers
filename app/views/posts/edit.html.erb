<h1>編集画面</h1>

<%= form_for(@post) do |f| %>

	<h4>title</h4>
	<%= f.text_field :title %>

	<h4>review</h4>
	<%= f.text_area :body %>

	<%= f.submit '保存' %>

<% end %>
<p>	<%= link_to "投稿一覧に戻る", posts_path %></p>
